<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.carrot.mapper.LogMapper">


    <select id="pageQuery" resultType="com.carrot.pojo.OperateLog">
        select operate_log.id,
               operate_emp_id,
               operate_time,
               class_name,
               method_name,
               method_params,
               return_value,
               cost_time,
               emp.username as operate_emp_name
        from operate_log
                 left join emp on operate_log.operate_emp_id = emp.id

    </select>
</mapper>

